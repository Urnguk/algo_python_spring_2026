# Контрольные вопросы к уроку 1
1) В случае, когда истинны утсверждения в блоке `if` и `elif` одновременно, который из них будет выполнен?
2) В каких случаях мы используем цикл `for`, а в каких `while`? 
3) Что происходит при присваивании в одну переменную содержимого другой (`y = x`)? Становятся ли они связанны?