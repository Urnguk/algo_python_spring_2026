# Контрольные вопросы к уроку 2
1) В каких случаях мы используем цикл `for`, а в каких `while`? 
2) Каким образом мы можем посчитать среднее арифметическое всех чисел последовательности за один проход по ней. Предложите способ без рассчета суммы всех чисел.
3) Напишите короткий код, позволяющий определить, что данное число `x` является пятизначным, и при этом оканчивается на _14_ и делится на _3_ без остатка.